# No-Ip update python script

```bash
$ python noip_update_public_ip.py -h

usage: noip_update_public_ip.py [-h] [--verbose] username password hostname

positional arguments:
  username
  password
  hostname

options:
  -h, --help  show this help message and exit
  --verbose
```